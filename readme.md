# RepoBrain

RepoBrain is an Agentic AI Repository Intelligence System.

The project is currently under incremental development.

## Current Phase

Phase 1 — Repository Scanner

The scanner:

- discovers repository files
- filters unnecessary directories and files
- detects languages
- identifies tests and configuration files
- collects file metadata
- calculates content hashes
- produces repository-level statistics

## Installation

```bash
python -m venv venv